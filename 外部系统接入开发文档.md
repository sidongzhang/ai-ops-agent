# 外部系统接入开发文档

本文档面向要接入 AIOps 平台的业务系统。接入后，外部系统可以在自己的页面展示平台消息、回写消息处理状态、上传错误日志生成告警、定期提交生产数据生成日报/周报。

## 1. 准备工作

在 AIOps 平台的系统详情页创建开放接口 Token，并记录：

- `BASE_URL`：平台地址，例如 `http://localhost:8000`
- `TOKEN`：系统 Token 明文，只显示一次
- `SYSTEM_CODE`：系统编码，例如 `order-system`

所有请求都要带：

```http
Authorization: Bearer <TOKEN>
X-System-Code: <SYSTEM_CODE>
```

建议 Token 权限：

- `message:read`：查询消息
- `message:write`：标记消息已读、确认、解决
- `log:alert`：上传错误日志并生成告警
- `log:analyze`：上传普通日志并分析
- `report:submit`：提交生产日报/周报数据
- `report:read`：查询报告结果

## 2. 查询消息并在外部系统展示

```bash
curl "$BASE_URL/openapi/v1/messages?limit=20&offset=0" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE"
```

返回结构：

```json
{
  "items": [
    {
      "id": 1,
      "message_type": "alert",
      "severity": "error",
      "title": "错误日志告警：error.log",
      "summary": "支付服务超时",
      "diagnosis": "原因分析...",
      "suggestion": ["检查支付网关", "查看重试队列"],
      "status": "unread",
      "related": {
        "request_id": "error-log-001",
        "processing_status": "done"
      },
      "created_at": "2026-07-15T08:00:00Z"
    }
  ],
  "total": 1,
  "offset": 0,
  "limit": 20
}
```

外部系统页面建议展示：

- 标题、系统、级别、时间
- `diagnosis` 原因分析
- `suggestion` 处理建议
- `status` 当前处理状态
- `related.processing_status` 后台分析状态

## 3. 标记消息已读、确认、解决

按 `request_id` 更新状态：

```bash
curl -X POST "$BASE_URL/openapi/v1/messages/error-log-001/status" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE" \
  -H "Content-Type: application/json" \
  -d '{"status":"acknowledged"}'
```

`status` 支持：

- `read`：已读
- `acknowledged`：已确认，表示有人接手
- `resolved`：已解决

## 4. 上传错误日志并生成告警

错误日志走专用接口。平台会：

1. 接收日志，返回 `202 Accepted`
2. 后台调用模型分析错误原因和处理建议
3. 生成 `alert` 消息写入消息中心
4. 按系统配置发送飞书、邮件、Webhook
5. 外部系统可用同一个 `request_id` 查询结果

```bash
curl -X POST "$BASE_URL/openapi/v1/error-logs" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE" \
  -F "file=@./error.log" \
  -F "request_id=error-log-001" \
  -F "question=请分析错误原因、影响范围和处理建议"
```

返回：

```json
{
  "request_id": "error-log-001",
  "system_id": 1,
  "message_id": 10,
  "filename": "error.log",
  "log_format": "文本日志",
  "status": "queued",
  "truncated": false,
  "detail": "日志已接收，平台将在后台分析，结果会回写到消息中心。"
}
```

查询分析后的告警：

```bash
curl "$BASE_URL/openapi/v1/messages/error-log-001" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE"
```

说明：

- 单个日志最大 `2 MB`
- 支持 UTF-8 文本或 JSON
- 原始日志不长期写入数据库
- 如果分析失败，外部系统可用同一个 `request_id` 重新上传

## 5. 提交生产日报数据

```bash
curl -X POST "$BASE_URL/openapi/v1/production/daily" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE" \
  -H "Content-Type: application/json" \
  -d '{
    "request_id": "daily-20260715",
    "title": "订单系统生产日报",
    "period": "2026-07-15",
    "summary": "订单处理 1000 笔，失败 3 笔",
    "data": {
      "total_orders": 1000,
      "success_orders": 997,
      "failed_orders": 3,
      "top_errors": [
        {"code": "PAY_TIMEOUT", "count": 2},
        {"code": "STOCK_LOCK_FAILED", "count": 1}
      ]
    },
    "context": {
      "env": "prod",
      "owner": "order-team"
    }
  }'
```

平台会后台加工为 `daily_report`，结合系统知识库生成生产日报，写入消息中心并发送通知。

## 6. 提交生产周报数据

```bash
curl -X POST "$BASE_URL/openapi/v1/production/weekly" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE" \
  -H "Content-Type: application/json" \
  -d '{
    "request_id": "weekly-2026-W29",
    "title": "订单系统生产周报",
    "period": "2026-W29",
    "summary": "本周订单整体稳定，失败率 0.3%",
    "data": {
      "total_orders": 7000,
      "failed_orders": 21,
      "avg_latency_ms": 186,
      "risk_items": ["周五晚高峰支付超时升高"]
    }
  }'
```

查询报告列表：

```bash
curl "$BASE_URL/openapi/v1/reports?report_type=daily&limit=20" \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-System-Code: $SYSTEM_CODE"
```

## 7. 接入建议

- `request_id` 必须由外部系统生成并保证幂等，例如 `error-log-订单号-时间戳`、`daily-YYYYMMDD`
- 外部页面不要直接展示原始日志，只展示平台返回的分析和建议
- 生产数据建议传结构化 JSON，不要传整段自然语言
- 定时任务建议：
  - 错误日志：实时或每 1 分钟聚合一次上传
  - 日报：每天固定时间上传
  - 周报：每周固定时间上传
- 外部系统展示消息时，用户点击“已读/确认/解决”后调用状态接口回写平台

## 8. 最小接入流程

1. 在平台创建系统和 Token
2. 外部系统保存 `BASE_URL / TOKEN / SYSTEM_CODE`
3. 页面定时调用 `/openapi/v1/messages`
4. 用户操作消息时调用 `/openapi/v1/messages/{request_id}/status`
5. 错误日志调用 `/openapi/v1/error-logs`
6. 生产日报调用 `/openapi/v1/production/daily`
7. 生产周报调用 `/openapi/v1/production/weekly`
