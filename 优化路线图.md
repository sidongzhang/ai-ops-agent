# AIOps 平台 · 后续发展优化路线图

> 制定日期：2026-09-22　基线：`94a5943`（融合改造计划五阶段完成，后端 99 测试全绿）
> 定位：以「Agent 算法能力」为主增长线，工程化/产品化为支撑线。
> 与《融合改造计划.md》（产品功能，已全部完成）和 `docs/resume/agent-简历包装与实现路线图.md`（算法细化论证）的关系：本文是合并后的执行总纲，冲突时以本文为准。

---

## 0. 核心原则

1. **评测先行**：任何 Agent 改动必须先有基准数字，改动后跑同一评测集回归。「变好了」必须有数字证明。
2. **每次提交都产出可复用资产**：数据集、指标、消融报告，而不是一次性脚本。
3. **不重造轮子**：线上指标复用 `analytics.py`，评测复用 `evals/`，降级复用现有三级链路。

---

## Phase 0 · 立基准（本周，1~3 天）

> 目标：把所有 `【待实测】` 变成真数字，让后续一切优化有对照。

| 事项 | 内容 | 产出 |
|---|---|---|
| 真跑评测 | `colima start && docker compose up -d` → `evals/run_eval.py --inject-all` → `--run --ablation all`（25 条 × 4 组消融） | 第一份**真实**消融报告（RCA 准确率 / 工具选择 F1 / 幻觉率 / 步数 / tokens） |
| 数据集扩展 | 25 → 100+ 条：每类故障做参数/服务名/干扰项变体，保持 20% 错误归因诱饵比例 | `evals/datasets/` 扩容 |
| CI 基线 | GitHub Actions：push 跑 `pytest tests`（99）+ `pytest evals/`（38）+ ruff + 前端 build | 绿色 CI 徽章 |
| 评测回归接入 | CI 每夜跑一次 `--dry-run` 链路校验；真跑评测（消耗额度）手动触发，报告提交到 `evals/results/`（gitignore 之外单独归档） | 回归机制 |

**验收**：有一张可粘贴的四组消融表；CI 全绿。

---

## Phase 1 · Agent 决策优化（Week 2~3）

> 目标：把「调 API」升级为「可度量的算法问题」。两项按性价比排序。

### 1.1 Playbook 语义路由（1~2 天）

- 现状：`skill_router.py` 关键词计数打分。
- 升级：Embedding 检索式路由（复用现有 `_embed`）+ 阈值拒识（无匹配 → 自由诊断）。
- 实验：关键词 vs Embedding 路由准确率对比（用评测集 `required_tools` 对齐作为标注）。
- **产出**：路由准确率提升数字 + 一次 Phase 0 评测回归。

### 1.2 结构化长期记忆（3~5 天，起点最高）

> 你已发现「记忆污染」问题（`diagnosis_auto_runbook` 默认关闭），这是最值钱的切入点。

- runbook 从全文追加升级为结构化故障记忆：`{症状, 上下文, 根因, 处置, 来源(模型/人工), 置信度, 时间}`。
- 检索按来源与置信度加权：人工确认 > 模型生成（随时间衰减）。
- **回查结果自动回填记忆有效性** → 带反馈的记忆闭环（打通现有 `verify_action_recovery_async`）。
- 加 rerank（API rerank 或 cross-encoder 小模型）。
- **产出**：记忆命中率 + 「Playbook+RAG vs baseline」RCA 准确率对比；开源记忆污染问题的解法叙事。

---

## Phase 2 · 效率与成本（Week 4~5）

> 目标：在成功率不降的前提下降 token 成本和步数。

### 2.1 Context Engineering（3~4 天）

- 工具返回长度自适应截断（替换硬编码 `[:1500]/[:2000]`）。
- 历史工具结果压缩：重复/无增益结果折叠为一行摘要。
- Token 预算管理器：按剩余预算决定「继续取证 vs 收口作答」。
- 冗余调用去重（评测里冗余调用率已达 34%，有明确优化空间）。
- **产出**：token 成本降低 __%、平均步数降低 __%，成功率不降（Phase 0 评测验证）。

### 2.2 MCP 化工具层（1~2 天，低成本高回报）

- 10 个工具抽成 MCP Server（`mcp` Python SDK），Agent 改为 MCP Client 动态发现。
- 新增连接器从「改 Agent 代码」降为「注册一个 MCP 工具」。

---

## Phase 3 · 上限项（Week 6~8，可选但强烈建议）

### 3.1 Agentic RL：GRPO 优化工具调用轨迹（1~2 周）

- 复用 RLVR 项目的 veRL/GRPO 基建，环境 = 本项目 AIOps 沙箱（评测集 + 故障注入即环境）。
- 奖励设计：`R = 1[根因正确] + α·1[必需工具] - β·冗余率 - γ·步数占比 - δ·1[无证据结论]`。
- 对照：SFT-only / GRPO / GRPO+过程奖励；做到「能跑通 + 有对照曲线」即可。

### 3.2 P2 增强项（穿插，按需）

- Plan-and-Execute + 反思：`plan → 并行取证 → 反思补证 → 结论`，降时延。
- Verifier 反驳式校验（第二个 Agent，专查「证据是否支持结论」，直接压幻觉率——有明确实验价值）。
- 工具结果缓存 + 熔断；Prompt 版本化 + 评测集 A/B。
- 长任务 checkpoint 持久化（MemorySaver → Postgres/Redis backend）。

---

## 支撑线 · 工程化与产品化（穿插进行，不占主线档期）

| 方向 | 事项 | 优先级 |
|---|---|---|
| 生产就绪 | Postgres 全量迁移验证 + 真实部署演练（非 dev）；数据库备份策略；HTTPS/Caddy 生产配置 | 高（有真实用户前必做） |
| 安全 | API 限流、审计日志保留策略、RBAC 角色细分（admin/operator/viewer） | 高 |
| 可观测 | Langfuse Span 级建模 + 失败模式分类统计（模型超限/认证/超时各占多少） | 中 |
| 前端 | 组件测试 + Playwright E2E（登录→注册系统→诊断→审批 最小链路） | 中 |
| 产品收尾 | 采集器自动升级机制、诊断报告导出（PDF/归档）、开放 API 文档站 | 低 |

---

## 执行顺序总览

```
Week 1      Phase 0 真跑评测 + 数据集扩展 + CI          ← 先有尺子和数字
Week 2~3    1.1 语义路由 → 1.2 结构化记忆               ← 有算法改进叙事
Week 4~5    2.1 Context Eng → 2.2 MCP                   ← 有成本数字
Week 6~8    3.1 Agentic RL（穿插 3.2 Verifier 实验最早做）← 上限项
穿插        支撑线（生产就绪优先于一切低优先级产品项）
```

**最小可行组合**（时间紧时）：Phase 0 + 1.1，即可得到「真实评测数据 + 一个算法改进点」。
**推荐组合**：Phase 0 + Phase 1 全部 + 2.2，性价比最高。
**上限组合**：全做，3.1 是论文级亮点。

---

## 度量看板（每阶段结束更新）

| 指标 | Phase 0 基线 | 当前 | 目标 |
|---|---|---|---|
| 任务成功率 % | 【待真跑】 | — | ≥ 85 |
| RCA 准确率 | 【待真跑】 | — | ≥ 0.9 |
| 幻觉率 % | 【待真跑】 | — | ≤ 10 |
| 平均工具调用数 | 【待真跑】 | — | ↓ 30%（冗余率 34% 是主要空间） |
| 平均 tokens | 【待真跑】 | — | ↓ 30%（Context Eng） |
| 数据集规模 | 25 | — | ≥ 100 |
| CI | 无 | — | 绿 |
